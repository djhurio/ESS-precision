# Options
options(max.print = 10e3)

# Install
if (FALSE) {
  pak::pkg_install(pkg = c(
    "purrr",
    "data.table",
    "openxlsx",
    "openxlsx2",
    "haven",
    "ggplot2"
  ))
}

# Packages
require(purrr)
require(data.table)
require(openxlsx)
require(openxlsx2)
require(haven)
require(ggplot2)
